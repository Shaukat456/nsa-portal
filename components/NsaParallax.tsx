import ParallaxText from "./ParallaxText";

export default function NsaParallax() {
  return (
    <main className="h-[300vh] bg-gradient-to-b from-blue-900 to-black overflow-hidden">
      <ParallaxText />
    </main>
  );
}
